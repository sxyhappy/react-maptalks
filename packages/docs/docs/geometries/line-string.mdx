---
sidebar_label: "LineString"
sidebar_position: 5
---

# LineString

## Example

```jsx live=true
function LineStringExample() {
  const view = { center: [-0.113049,51.49856], pitch: 0, zoom: 14 };
  const [visible, setVisible] = useState(true);
  const lineStringConfig = {
    symbol: {
      lineColor: '#34495e',
      lineWidth: 2,
    },
    coordinates: [
      [-0.131049, 51.498568],
      [-0.107049, 51.498568]
    ],
  }

  return (
    <>
      <button onClick={() => setVisible(!visible)}>change visible</button>
      <MtMap {...view} style={{height: 400}}>
        <MtTileLayer
          id={"base"}
          urlTemplate="https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}.png"
          subdomains={['a','b','c','d']}
          attribution={'&copy; <a href="http://osm.org">OpenStreetMap</a> contributors, &copy; <a href="https://carto.com/">CARTO</a>'}
        />

        <MtVectorLayer id={'vector'}>
          <MtLineString {...lineStringConfig} visible={visible} />
        </MtVectorLayer>
      </MtMap>
    </>
  )
}

```
